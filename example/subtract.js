/**
 * Subtract two numbers.
 * @param {string | number} a The first number.
 * @param {string | number} b The second number.
 * @returns The difference of a and b.
 */
export const subtract = (a, b) => +a - +b
